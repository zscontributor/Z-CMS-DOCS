---
title: 本番環境へのデプロイ
description: Z-CMS を本番環境で運用するために必要なコンポーネントとセキュリティ要件を説明します。
sidebar:
  order: 1
---

本番環境は、API、サイトランタイム、Admin、プラグインランタイム、worker、PostgreSQL、Redis、S3 互換オブジェクトストレージで構成されます。

## チェックリスト

- サービスごとに異なる認証情報を使用します。
- アプリケーション用データベースロールが `NOBYPASSRLS` で、テーブル所有者ではないことを確認します。
- プラグインランタイムにデータベース、S3、セッションの認証情報を渡しません。
- 信頼できる情報源から取得した `MARKETPLACE_PUBLIC_KEY` を固定して使用します。
- 公開するすべてのホスト名で TLS を有効にします。
- 新しいバージョンへトラフィックを切り替える前にマイグレーションを実行します。
- バックアップを設定し、定期的に復元テストを行います。

:::danger
`APP_DATABASE_URL` にデータベース所有者のロールを使用しないでください。テーブル所有者は Row-Level Security を迂回できるため、テナント分離が失われます。
:::
